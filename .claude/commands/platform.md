# /platform

Use the MGD Project Platform System rules from this repository.

Arguments: `$ARGUMENTS`

If no mode is supplied, start with a read-only audit.

Read `AGENTS.md`, `WIKI/README.md` and `platform/SKILL.md` before acting.
