/* Eigenes JavaScript. Im Backoffice unter "Code-Editoren" bearbeitbar (mit automatischer Sicherung).
   Externe Dienste/Tracking nur nach Einwilligung laden:
   document.addEventListener('mgd:consent', function (event) { if (event.detail.choice === 'all') { ... } }); */
